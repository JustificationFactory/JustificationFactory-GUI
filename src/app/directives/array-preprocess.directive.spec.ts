import { ArrayPreprocessDirective } from './array-preprocess.directive';

describe('ArrayPreprocessDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrayPreprocessDirective();
    expect(directive).toBeTruthy();
  });
});
